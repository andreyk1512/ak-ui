# Introduction
**ak-ui** is a typed React UI kit library.

## Installation
You can install the library using npm, yarn or pnpm:

```shell
npm install -S ak-ui
```
```shell
yarn add ak-ui
```
```shell
pnpm add ak-ui
```
